# Base URL of the backend REST API + Socket.IO server.
VITE_API_URL="http://localhost:4000"

# Id of the board to render (matches the seeded default board).
VITE_BOARD_ID="11111111-1111-1111-1111-111111111111"
